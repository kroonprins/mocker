name: echo post
request:
  path: /echo
  method: POST
response:
  templatingEngine: nunjucks
  contentType: application/json
  statusCode: 200
  headers:
    - name: a
      value: "b {{req.query.q}}"
  body: |
    { "a": "b {{req.query.q}}" }
